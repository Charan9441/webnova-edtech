{
	"name": "webnova-functions",
	"version": "1.0.0",
	"private": true,
	"engines": { "node": "^18" },
	"scripts": {
		"serve": "firebase emulators:start --only functions",
		"deploy": "firebase deploy --only functions",
		"lint": "echo 'no lint'"
	},
	"dependencies": {
		"firebase-admin": "^12.6.0",
		"firebase-functions": "^5.1.1",
		"luxon": "^3.5.0"
	}
}

